export default function OurApproach() {
  return (
    <section className="flex w-full flex-col items-center px-8 pt-24">
      <div className="mb-16 flex w-full max-w-3xl flex-col">
        <h4 className="mb-4 font-semibold dark:text-slate-50">{`Our approach`}</h4>
        <p>
          {`We interacted with the original design as customers and tested each area of the app. After multiple tests, we identified two main flows that can benefit from a redesign: the Search and Checkout flows.`}
        </p>
      </div>
      <div className="mb-16 flex w-full max-w-3xl flex-col">
        <h4 className="mb-4 font-semibold dark:text-slate-50">{`Our approach`}</h4>
        <p>
          {`We used the app as customers and tested each area of the app. After multiple tests, we identified two main flows that can benefit from a redesign: the Search and Checkout flows.`}
        </p>
      </div>
    </section>
  );
}
